# requirements-dev.txt — outillage dev, test et CI.
#
# Séparé du « main » (requirements.txt) : ces paquets ne sont PAS nécessaires
# pour faire tourner la démo. La CI installe les deux fichiers.
#
# Environnement complet :
#     pip install -r requirements.txt -r requirements-dev.txt

# --- Lint & tests ------------------------------------------------------------
ruff>=0.6.0                 # PEP 8 + pydocstyle + isort — bloquant en CI
pytest>=8.0.0               # suite de tests rapide

# --- Moteur BERT (Approche 4) ------------------------------------------------
# Installe PyTorch + Sentence-Transformers (~2 Go).
# Nécessaire pour les tests du moteur BERT et pour le faire tourner en local.
torch>=2.0.0
sentence-transformers>=3.0.0

# --- Évaluation IA (optionnel, lourd) ----------------------------------------
# Décommenter pour lancer la suite d'évaluation complète.
# deepeval>=1.0.0
# giskard>=2.14.0            # Python < 3.12 uniquement
# vl-convert-python>=1.0.0   # rendu PNG des figures Vega-Lite
