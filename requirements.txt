# Runtime dependencies for the Déraison Assurances intent engine.
# pip-installable mirror of [project.dependencies] in pyproject.toml.
# pyproject.toml is the source of truth; keep both in sync.

# --- Core web + config ---------------------------------------------------
fastapi>=0.115.0            # HTTP API serving the three engines
uvicorn[standard]>=0.32.0   # ASGI server for the API
pydantic>=2.10.0            # request/response models
pydantic-settings>=2.6.0    # environment-backed Settings
python-multipart>=0.0.20    # form parsing (future file uploads)

# --- Classic ML (Approach 1 TF-IDF, Approach 2 classifier) ---------------
scikit-learn>=1.5.0         # TfidfVectorizer + LogisticRegression
numpy>=1.26.0               # array math shared by both classifiers

# --- fastText engine (Approach 2 — trained on our examples) --------------
fasttext-wheel>=0.9.2      # prebuilt fastText (no compiler needed)

# --- LLM + embeddings backends (Approach 5, and BERT via Ollama) ---------
httpx>=0.27.0              # synchronous Ollama REST client

# --- Optional heavy backend (Approach 4, the SBERT + PyTorch MLP path) ---
# sentence-transformers pulls PyTorch (~2 GB); torch also runs the BERT
# engine's MLP head. Install to enable the BERT engine on the SBERT path:
#     pip install "sentence-transformers>=3.0.0" torch
#
# The pretrained fastText engine (Approach 3) needs the ~4.5 GB cc.fr.300
# model, downloaded on demand (see the README / scripts).
