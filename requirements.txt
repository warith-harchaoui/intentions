# requirements.txt — dépendances pour FAIRE TOURNER le moteur d'intention.
#
# Installation rapide (suffit pour lancer la démo) :
#     pip install -r requirements.txt
#
# Environnement complet dev/test/BERT/eval :
#     pip install -r requirements.txt -r requirements-dev.txt
#
# Le moteur BERT (~2 Go) et le moteur fastText pré-entraîné (~7 Go sur disque)
# nécessitent des étapes supplémentaires décrites dans le README.

# --- Serveur & API -----------------------------------------------------------
fastapi>=0.115.0            # API web (cinq moteurs + comparateur)
uvicorn[standard]>=0.32.0   # serveur ASGI
pydantic>=2.10.0            # modèles de requête / réponse
pydantic-settings>=2.6.0    # configuration via variables d'environnement
python-multipart>=0.0.20    # parsing de formulaires

# --- Moteurs ML (Approches 1, 2, 3) -----------------------------------------
scikit-learn>=1.5.0         # TF-IDF + Random Forest + régression logistique
numpy>=1.26.0               # calcul matriciel partagé

# --- Moteur fastText appris (Approche 2) -------------------------------------
fasttext-wheel>=0.9.2       # fastText précompilé (pas de compilateur requis)

# --- Client Ollama (Approche 5 LLM, et repli embeddings pour BERT) -----------
httpx>=0.27.0               # client REST synchrone vers Ollama

# --- Détection de langue & i18n ----------------------------------------------
langdetect>=1.0.9           # détecte la langue de la requête (EN/FR)
PyYAML>=6.0                 # table i18n : locales/i18n.yaml

# --- Évaluation (métriques légères, sans LLM) --------------------------------
skore>=0.23.0               # CrossValidationReport / ComparisonReport
skrub>=0.10.0               # pipeline sklearn déclaratif
